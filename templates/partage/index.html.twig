{% extends 'base.html.twig' %}

{% block title %}ListType{% endblock %}

{% block body %}
    <h1>ListType</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ list_type.id }}</td>
        </tr>
        <tr>
            <th>Titre</th>
            <td>{{ list_type.Titre }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ list_type.Description }}</td>
        </tr>
        <tr>
            <th>Couverture</th>
            <td>{{ list_type.Couverture }}</td>
        </tr>
        <tr>
            <th>Theme</th>
            <td>{{ list_type.Theme }}</td>
        </tr>
        <tr>
            <th>Status</th>
            <td>{{ list_type.Status }}</td>
        </tr>
        <tr>
            <th>Date_ouvert</th>
            <td>{{ list_type.dateOuvert }}</td>
        </tr>
        <tr>
            <th>Date_fin</th>
            <td>{{ list_type.dateFin }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_list_type_index') }}">back to list</a>

    <a href="{{ path('app_list_type_edit', {'id': list_type.id}) }}">edit</a>

    {{ include('list_type/_delete_form.html.twig') }}
{% endblock %}
